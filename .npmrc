# Use a flat node_modules so packages that rely on hoisted/peer deps
# (better-sqlite3 native build, ipfs-only-hash's transitive deps, chakra, etc.)
# resolve the same way they did under npm.
node-linker=hoisted
